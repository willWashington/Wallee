create a data factory to pull in all the api request results and store them locally every X minutes
local storage for Chrome can be determined here:
https://arty.name/localstorage.html

apod is ready
need to ask if user wants it
build data factory to store api data to restrict call volume

//STRIKE: build swap controller to swap pages on a timer

rebuild entire project to display on a single page with fades
    this allows for better control of API calls and no unnecessary page swaps

at least change to jquery for fade effect, but perhaps study react to see if it will allow better implementation

maybe implement class-based firing by understanding better this blog: 
https://www.paulirish.com/2009/markup-based-unobtrusive-comprehensive-dom-ready-execution/
